@using Projeto_Contabilidade.Client.Pages.Contato
@inject NavigationManager NavigationManager

<div class="table__header--title">
    <h2 class="table__title">Contatos Cadastrados</h2>
    <div class="table__search">
        <span class="oi oi-magnifying-glass"></span>
        <input placeholder="Pesquisa" class="table__search--input" />
    </div>
</div>
<table class="table">
    @foreach (ContatoViewModel contato in contatos)
    {
        <tr @onclick=@(() => NavigationManager.NavigateTo("InformacaoContato")) class="table__line">
            <td class="table__data">
                @contato.Nome_Contato
            </td>
            <td class="table__data">
                @contato.Email
            </td>
            <td class="table__data">
                <button @onclick=@(() => NavigationManager.NavigateTo("CadastroContato")) class="btn btn-outline-success">Editar</button>
                <button class="btn btn-outline-danger">Deletar</button>
            </td>
        </tr>
    }
    <tr class="table__line">
        <td colspan=3 class="justify-content-center">
            <button @onclick=@(() => NavigationManager.NavigateTo("CadastroContato")) class="btn btn-outline-primary">
                Novo Contato
            </button>
        </td>
    </tr>
</table>

@code {
    [Parameter] public List<ContatoViewModel> contatos { get; set; }
    [Parameter] public string[] tableData { get; set; }
    [Parameter] public string tableLink { get; set; }
}
