@using Projeto_Contabilidade.Client.Componentes.Botao
@inject NavigationManager NavigationManager

<div class="table__header--title">
    <h2 class="table__title">Contatos Cadastrados</h2>
    <div class="table__search">
        <span class="oi oi-magnifying-glass"></span>
        <input placeholder="Pesquisa" class="table__search--input" />
    </div>
</div>
<table class="table">
    <tr class="table__line">
        @foreach (string data in tableData)
        {
            <td class="table__data">
                @data
            </td>
        }
        <td class="table__data">
            <button @onclick=@(() => NavigationManager.NavigateTo("InformacaoContato")) class="button button--small button--edit">Editar</button>
            <button class="button button--small button--delete">Deletar</button>
        </td>
    </tr>
    <tr class="table__line">
        <td colspan=3 class="table__data">
            <button @onclick=@(() => NavigationManager.NavigateTo("CadastroContato")) class="button button--action">
                Novo Contato
            </button>
        </td>
    </tr>
</table>

@code {
    [Parameter] public string[] tableData { get; set; }
    [Parameter] public string tableLink { get; set; }
}
